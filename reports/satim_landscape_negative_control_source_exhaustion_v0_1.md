# SATIM Landscape Negative-Control Source Sweep v0.1

Status: SOURCE_DENOMINATOR_CLOSED / RAW_FIXTURE_DENOMINATOR_BLOCKED

All 12 declared negative-control classes have at least one public source candidate
with source-description and reuse-license context recorded in
`data/satim_landscape/negative_control_source_registry_v0_1.json`.

None counts as a benchmark fixture yet. Exact raw bytes are not frozen in this
repository, SHA-256 identities do not exist, and several classes require an explicit
ROI adjudication before their source scene can be promoted to verified ground truth.
Source taxonomy is not canonical fixture identity.

The benchmark therefore remains BLOCKED. This is deliberate fail-closed behavior.
Production promotion must not occur from source-page availability alone.
