# Compatibility installer for existing setup instructions.
# All dependency ownership now lives in pyproject.toml and shared federation
# packages are pinned to an immutable thehub-pr commit.
-e .[federation]
