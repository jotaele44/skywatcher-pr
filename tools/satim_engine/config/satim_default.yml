version: v21
pairing:
  time_window_minutes: 30
  # RESERVED / not yet evaluated: visual metadata does not currently carry
  # coordinates, so pairing.py cannot apply a spatial gate. This value is a
  # placeholder for when visual records gain lat/lon; it is a no-op today.
  spatial_threshold_meters: 1500
  confidence_threshold_promote: 80
scoring:
  verified: 95
  high_confidence: 80
  approximate: 60
