# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtualenvs
.venv/
venv/

# macOS / session junk
.DS_Store
.remember/

# Local airspace data / outputs (only the synthetic example is tracked)
*.db
outputs/
tile_cache/

# generated canonical export (scripts/federation_export.py)
exports/federation/

# FR24 ingest data — large baseline corpus + run manifests.
# These live in spiderweb-pr and are wired in via relative symlinks during the
# migration transition (skywatcher owns the FR24 pipeline; data is not committed).
data/FR24_baseline
data/_manifests/
flight_database.db
