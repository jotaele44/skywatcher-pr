# GEO-PR-INT requirements
# Core scientific stack
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Geospatial
pyproj>=3.6.0
geopandas>=0.14.0
shapely>=2.0.0

# HTTP / APIs
requests>=2.31.0

# Config
pyyaml>=6.0

# API server (optional — only needed for --serve)
fastapi>=0.100.0
uvicorn>=0.23.0

# Database (optional — only needed for PostGIS writes)
sqlalchemy>=2.0.0

# Visualization (optional — needed for pr_intelligence_system map step)
folium>=0.15.0
branca>=0.7.0

# pr_intelligence_system satellite deps (used via sys.path integration)
boto3>=1.34.0
botocore>=1.34.0
requests>=2.31.0
netCDF4>=1.6.5
