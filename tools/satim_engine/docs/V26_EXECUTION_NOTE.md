# V26 execution note

This patch closes the final known stable-ID gap identified after the v24 hotfix merge.

The graph builder now uses source-local ordinals rather than batch-global DataFrame row indexes.
